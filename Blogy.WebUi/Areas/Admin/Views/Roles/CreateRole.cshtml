@model AppRole

@{
    ViewData["Title"] = "Rol Oluştur";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-3">
        <div class="card-header bg-primary text-white text-center">
            <h4 class="mb-0 fw-semibold">🛡️ Rol Oluştur</h4>
        </div>

        <div class="card-body p-4">
            <form method="post">
                <div class="mb-3">
                    <label asp-for="Name" class="form-label fw-semibold">Rol Adı</label>
                    <input type="text" class="form-control" asp-for="Name" placeholder="Rol Adını Giriniz" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary px-4 fw-semibold">
                        Oluştur
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
