@{
    Layout = null;
}

@* Views/Shared/_AdminLayout.cshtml *@
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Admin Panel</title>
    
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
    <style>
        :root {
            --bg: #0f172a;
            --sidebar: #1e293b;
            --nav: #111827;
            --text: #e2e8f0;
            --muted: #94a3b8;
            --accent: #10b981;
            --border: rgba(255,255,255,0.05);
        }

        body {
            margin: 0;
            font-family: "Segoe UI", Roboto, sans-serif;
            color: var(--text);
            background-color: var(--bg);
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        /* === SIDEBAR === */
        .sidebar {
            width: 240px;
            background: var(--sidebar);
            display: flex;
            flex-direction: column;
            border-right: 1px solid var(--border);
            transition: width 0.3s;
        }

        .sidebar-header {
            font-size: 20px;
            font-weight: 600;
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid var(--border);
            color: var(--accent);
        }

        .nav-links {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            padding: 10px 0;
        }

            .nav-links a {
                display: flex;
                align-items: center;
                gap: 12px;
                color: var(--muted);
                text-decoration: none;
                padding: 12px 24px;
                transition: all 0.25s;
            }

                .nav-links a:hover,
                .nav-links a.active {
                    background: rgba(16, 185, 129, 0.1);
                    color: var(--accent);
                }

            .nav-links i {
                width: 18px;
                text-align: center;
            }

        .logout {
            border-top: 1px solid var(--border);
            padding: 16px 24px;
            font-size: 14px;
            color: #f87171;
            cursor: pointer;
        }

        /* === MAIN AREA === */
        .main {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .navbar {
            background: var(--nav);
            border-bottom: 1px solid var(--border);
            padding: 12px 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

            .navbar h1 {
                font-size: 18px;
                font-weight: 600;
                margin: 0;
            }

            .navbar .user-info {
                display: flex;
                align-items: center;
                gap: 12px;
                color: var(--muted);
            }

                .navbar .user-info i {
                    color: var(--accent);
                }

        .content {
            flex-grow: 1;
            overflow-y: auto;
            padding: 30px;
        }

        .footer {
            text-align: center;
            padding: 15px;
            font-size: 13px;
            color: var(--muted);
            border-top: 1px solid var(--border);
        }

        /* === SCROLLBAR === */
        ::-webkit-scrollbar {
            width: 6px;
        }

        ::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
        }

        @@media (max-width: 768px) {
            .sidebar {
                position: fixed;
                z-index: 1000;
                height: 100%;
                left: -240px;
            }

                .sidebar.open {
                    left: 0;
                }
        }
    </style>
</head>

<body>
    <aside class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <i class="fa-solid fa-shield-halved"></i> Admin Panel
        </div>
        <nav class="nav-links">
            <a href="/Admin/Dashboard" class="active"><i class="fa-solid fa-chart-line"></i> Dashboard </a>
            <a href="/Admin/User"><i class="fa-solid fa-users"></i> Kullanıcılar </a>
            <a href="/Admin/Blog"><i class="fa-solid fa-newspaper"></i> Bloglar </a>
            <a href="/Admin/Contact"><i class="fa-solid fa-envelope"></i> Gelen Mesajlar </a>
            <a href="/Admin/Comment"><i class="fa-solid fa-comments"></i> Yorumlar </a>
            <a href="/Admin/Profile"><i class="fa-solid fa-user-circle"></i> Profil işlemleri </a>
            <a href="/Admin/ChangePassword"><i class="fa-solid fa-key"></i> Şifre Değiştirme </a>
        </nav>
        <div class="logout" onclick="location.href='/Login/Logout'">
            <i class="fa-solid fa-right-from-bracket"></i> Çıkış Yap
        </div>
    </aside>

    <main class="main">
        <div class="navbar">
            <h1>@ViewData["Title"]</h1>
            <div class="user-info">
                <i class="fa-solid fa-user-shield"></i> @User.Identity?.Name
            </div>
        </div>

        <div class="content">
            @RenderBody()
        </div>

        <footer class="footer">
            © 2025 Admin Panel - Tüm Hakları Saklıdır.
        </footer>
    </main>

    <script>
        // Mobilde sidebar açma/kapama (isteğe bağlı)
        const sidebar = document.getElementById('sidebar');
        document.addEventListener('keydown', e => {
            if (e.key === 'Escape') sidebar.classList.remove('open');
        });
    </script>
</body>
</html>
